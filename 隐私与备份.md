# 隐私与备份

## 当前决定

- 当前 Codex 模型端点保留现有配置；因此把本仓库视为可能经过第三方服务处理的数据环境。
- 不把高敏个人信息写入仓库或对话：身份证号、账号密码、手机号、详细住址、家庭财务、医疗隐私、完整学号。
- 成绩、排名和目标院校可记录，但只记录规划所需的最小信息。

## 备份策略

- PDF 原件使用个人私有云盘备份，不提交 GitHub。
- GitHub 私有仓库只保存 Markdown、Skill、脚本和可检索文本。
- 推送前检查：`git diff --cached --stat`、`git status --short`，确认没有 PDF、令牌或隐私字段。
- 远端仓库：`skyfrostz/NCU-StudyRocket`，必须保持 Private。

## 本地应用

- `/Applications/NCU StudyRocket.app` 不保存模型令牌、不启动本地服务器、不创建登录项或常驻后台进程。
- 应用备份写入 `~/Library/Application Support/NCU StudyRocket/Backups/`，不进入仓库；每个文件保留最近 20 份旧版本。
- 应用只允许写入当前仓库根目录内的 `.md` 文件；PDF 仍只读并由私有云盘备份。
- 每日行为账保存后，应用仅把五项已填事实摘要写入 `工作台/助理偏好与习惯.md`；不采集页面点击、功能使用次数、情绪、账号或高敏信息。
- `工作台/助理偏好与习惯.md` 的“计划阻力规律”只允许记录匿名、可操作且至少出现 3 次的模式；具体情绪措辞、严重程度、私人原因不会落盘。
- 迁移仓库后检查应用显示的路径，避免把个人资料误绑定到其他目录。

## 学业对话隔离

- 应用固定续接名为“StudyRocket 学业助理”的独立 Codex 任务；当前开发任务不会被读取，也不使用“最近对话”。
- 学业任务使用只读沙箱；联网只用于按仓库规则核实官方时效信息。计划、复盘和档案修改必须先形成应用内草案，用户确认后才写入 Markdown。
- 应用仅在聊天期间启动本机 Codex app-server，退出聊天/应用后释放子进程；不保存 API Key、OAuth 令牌或聊天副本到仓库。
- 现有第三方模型端点继续沿用本机 Codex 配置，聊天内容可能经过该端点处理；不要在对话中输入高敏个人信息。
- Skill 修改只能由周复盘提出差异草案并经用户确认；应用限制在仓库八个现有 `SKILL.md`，不会写入全局 Skill 或任意凭证配置。

## 通知迁移

- 原生通知通过 macOS `UNUserNotificationCenter` 登记，日、周、月三类提醒相互独立。
- 旧 Codex Scheduled Tasks 在原生通知、点击路由和学业对话验收前保持启用；确认连续两次运行正常后再停用旧任务，不删除配置。
